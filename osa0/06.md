```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server
    participant Database

    User->>Browser: muistiinpano ja paina Save
    Browser->>Browser: Päivitä local state
    Browser->>Server: POST /exampleapp/new_note_spa { content, date }
    activate Server
    Server->>Database: note(content, date)
    activate Database
    Database-->>Server: OK, id=99
    deactivate Database
    Server-->>Browser: HTTP 201 { message:"note created", id:99 }
    deactivate Server

    Browser->>Browser: Lisää uusi muistiinpano
    Browser->>Browser: Renderöi päivitetty lista 
    Browser->>User: Näytä päivitetty lista muistiinpanoista
